import React from "react";
import { ScrollView, Text, View } from "react-native";

export default function SpecialistCardComponent({ data }) {
  return (
    <ScrollView horizontal>
      <Text>Salam</Text>
    </ScrollView>
  );
}
